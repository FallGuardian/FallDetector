<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="multiple.by.patience"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" >
    </uses-permission>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="multiple.by.patience.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="multiple.by.patience.SettingActivity"
            android:label="@string/title_activity_setting" >
        </activity>
        <activity
            android:name="multiple.by.patience.WarningActivity"
            android:label="@string/title_activity_warning" >
        </activity>
        <activity
            android:name="multiple.by.patience.ContactActivity"
            android:label="@string/title_activity_contact" >
        </activity>

        <service android:name="multiple.by.patience.Acconly" >
        </service>

        <activity
            android:name="multiple.by.patience.User_info"
            android:label="@string/title_activity_user_info" >
        </activity>
        <activity
            android:name="multiple.by.patience.Intermediate"
            android:label="@string/title_activity_intermediate" >
        </activity>
        <activity
            android:name="multiple.by.patience.WelcomeActivity"
            android:label="@string/title_activity_welcome" >
        </activity>

        <receiver android:name="multiple.by.patience.AlarmReceiver" >
        </receiver>

        <activity
            android:name="multiple.by.patience.User_posActivity"
            android:label="@string/title_activity_user_pos" >
        </activity>
        <activity
            android:name="multiple.by.patience.PosActivity"
            android:label="@string/title_activity_pos" >
        </activity>
    </application>

</manifest>